## 2025-02-18 - FAB Menu Separation
**Learning:** FAB menus sharing the same `bottom` position as their trigger cause severe overlap.
**Action:** Position FAB menus with distinct vertical separation (e.g., `bottom-24` vs `bottom-6`) and enforce `aria-label` on icon-only triggers.
